name: Build Physical Keyboard Toolbar Test

on:
  push:
    branches: ["test/physical-keyboard-toolbar"]
    paths:
      - "android_toolbar_test_source.zip"
      - ".github/workflows/build-physical-keyboard-toolbar-test.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Extract source
        run: |
          rm -rf /tmp/PhysicalKeyboardToolbarTest
          unzip -q android_toolbar_test_source.zip -d /tmp
      - name: Build debug APK
        working-directory: /tmp/PhysicalKeyboardToolbarTest
        run: gradle --no-daemon :app:assembleDebug
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: PhysicalKeyboardToolbarTest-debug
          path: /tmp/PhysicalKeyboardToolbarTest/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
